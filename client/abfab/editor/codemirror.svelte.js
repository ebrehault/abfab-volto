/* generated by Svelte v3.38.3 */
import {
	SvelteComponent,
	append,
	attr,
	binding_callbacks,
	detach,
	element,
	init as init_1,
	insert,
	noop,
	safe_not_equal,
	space
} from "/~/libs/svelte/internal/index.mjs";

import { onMount } from "/~/libs/svelte/index.mjs";
import { createEventDispatcher } from "/~/libs/svelte/index.mjs";

function add_css() {
	var style = element("style");
	style.id = "svelte-11exmak-style";
	style.textContent = "textarea.svelte-11exmak{display:none}.CodeMirror{width:100%;height:calc(100vh - 40px) !important}";
	append(document.head, style);
}

function create_fragment(ctx) {
	let textarea_1;
	let t;
	let link0;
	let link1;

	return {
		c() {
			textarea_1 = element("textarea");
			t = space();
			link0 = element("link");
			link1 = element("link");
			attr(textarea_1, "class", "svelte-11exmak");
			attr(link0, "rel", "stylesheet");
			attr(link0, "href", "https://www.unpkg.com/codemirror@5.63.3/lib/codemirror.css");
			attr(link1, "rel", "stylesheet");
			attr(link1, "href", "https://www.unpkg.com/codemirror@5.63.3/theme/blackboard.css");
		},
		m(target, anchor) {
			insert(target, textarea_1, anchor);
			/*textarea_1_binding*/ ctx[5](textarea_1);
			insert(target, t, anchor);
			append(document.head, link0);
			append(document.head, link1);
		},
		p: noop,
		i: noop,
		o: noop,
		d(detaching) {
			if (detaching) detach(textarea_1);
			/*textarea_1_binding*/ ctx[5](null);
			if (detaching) detach(t);
			detach(link0);
			detach(link1);
		}
	};
}

async function loadPlugins() {
	await import("https://www.unpkg.com/codemirror@5.63.3/addon/mode/simple.js");
	await import("https://www.unpkg.com/codemirror@5.63.3/addon/mode/multiplex.js");
	await import("https://www.unpkg.com/codemirror@5.63.3/mode/javascript/javascript.js");
	await import("https://www.unpkg.com/codemirror@5.63.3/mode/handlebars/handlebars.js");
	await import("https://www.unpkg.com/codemirror@5.63.3/mode/htmlmixed/htmlmixed.js");
	await import("https://www.unpkg.com/codemirror@5.63.3/mode/xml/xml.js");
	await import("https://www.unpkg.com/codemirror@5.63.3/mode/css/css.js");
	await import("https://www.unpkg.com/codemirror@5.63.3/mode/markdown/markdown.js");
	await import("https://www.unpkg.com/codemirror@5.63.3/addon/edit/closebrackets.js");
	await import("https://www.unpkg.com/codemirror@5.63.3/addon/edit/closetag.js");
	await import("https://www.unpkg.com/codemirror@5.63.3/addon/edit/continuelist.js");
	await import("https://www.unpkg.com/codemirror@5.63.3/addon/comment/comment.js");
	await import("https://www.unpkg.com/codemirror@5.63.3/addon/fold/foldcode.js");
	await import("https://www.unpkg.com/codemirror@5.63.3/addon/fold/foldgutter.js");
	await import("https://www.unpkg.com/codemirror@5.63.3/addon/fold/brace-fold.js");
	await import("https://www.unpkg.com/codemirror@5.63.3/addon/fold/xml-fold.js");
	await import("https://www.unpkg.com/codemirror@5.63.3/addon/fold/indent-fold.js");
	await import("https://www.unpkg.com/codemirror@5.63.3/addon/fold/markdown-fold.js");
	await import("https://www.unpkg.com/codemirror@5.63.3/addon/fold/comment-fold.js");
}

function instance($$self, $$props, $$invalidate) {
	let { content } = $$props;
	let textarea;
	let codeMirror;
	let isInitialized = false;
	const dispatch = createEventDispatcher();

	const modes = {
		js: { name: "javascript", json: false },
		json: { name: "javascript", json: true },
		svelte: { name: "handlebars", base: "text/html" },
		md: { name: "markdown" }
	};

	function saveFile(value) {
		if (!value) {
			value = codeMirror.getValue();
		}

		dispatch("save", value);
	}

	onMount(async () => {
		$$invalidate(4, isInitialized = false);
		await import("https://www.unpkg.com/codemirror@5.63.3/lib/codemirror.js");
		await loadPlugins();
		init();
	});

	function init() {
		const opts = {
			lineNumbers: true,
			lineWrapping: true,
			indentWithTabs: false,
			indentUnit: 4,
			tabSize: 4,
			value: "",
			autoCloseBrackets: true,
			autoCloseTags: true,
			extraKeys: {
				"Enter": "newlineAndIndentContinueMarkdownList",
				"Ctrl-/": "toggleComment",
				"Cmd-/": "toggleComment",
				"Ctrl-Q"(cm) {
					cm.foldCode(cm.getCursor());
				},
				"Cmd-Q"(cm) {
					cm.foldCode(cm.getCursor());
				},
				"Ctrl-S"(cm) {
					saveFile(cm.getValue());
				},
				"Cmd-S"(cm) {
					saveFile(cm.getValue());
				}
			},
			foldGutter: true,
			gutters: ["CodeMirror-linenumbers", "CodeMirror-foldgutter"],
			theme: "blackboard"
		};

		$$invalidate(3, codeMirror = CodeMirror.fromTextArea(textarea, opts));
		$$invalidate(4, isInitialized = true);
	}

	

	function setMode() {
		const filename = location.pathname.replace("/@edit", "").split("/").pop();

		const mode = filename.includes(".")
		? filename.split(".").pop()
		: "json";

		codeMirror.setOption("mode", modes[mode] || { name: mode });
	}

	function textarea_1_binding($$value) {
		binding_callbacks[$$value ? "unshift" : "push"](() => {
			textarea = $$value;
			$$invalidate(0, textarea);
		});
	}

	$$self.$$set = $$props => {
		if ("content" in $$props) $$invalidate(1, content = $$props.content);
	};

	$$self.$$.update = () => {
		if ($$self.$$.dirty & /*isInitialized, codeMirror, content*/ 26) {
			$: if (isInitialized) {
				codeMirror.setValue(content);
				setMode();
			}
		}
	};

	return [textarea, content, saveFile, codeMirror, isInitialized, textarea_1_binding];
}

class Component extends SvelteComponent {
	constructor(options) {
		super();
		if (!document.getElementById("svelte-11exmak-style")) add_css();
		init_1(this, options, instance, create_fragment, safe_not_equal, { content: 1, saveFile: 2 });
	}

	get saveFile() {
		return this.$$.ctx[2];
	}
}

export default Component;